@model List<Category>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/AdminArea/Views/Shared/_AdminLayout.cshtml";
    int count = 1;
}

<div class="container">
    <div class="row">
        <div class="justify-content-between d-flex mt-3 justify-content-between" style="width:100%;">
            <div>
                <h1>Categories</h1>
            </div>
            <div>
                <a class="btn btn-success" asp-controller="category" asp-action="create">Create Category</a>
            </div>
        </div>
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>
                            Number
                        </th>
                        <th>
                            Category Image
                        </th>
                        <th>
                            Category Name
                        </th>
                        <th>
                            Settings
                        </th>

                    </tr>
                </thead>
                <tbody>
                    @foreach (var category in Model)
                    {
                        <tr>
                            <td>
                                @count
                            </td>
                            <td>
                                <img src="~/Assets/img/categoriesMainPictures/@category.Image" alt="Alternative Image" style="width:100px; height:100px;border-radius:10%;" />

                            </td>
                            <td>
                                @category.Name
                            </td>
                            <td>
                                <a class="btn btn-primary btn-sm" asp-controller="category" asp-action="Detail" asp-route-categoryId="@category.Id">
                                    <i class="mdi mdi-account-box-outline"></i>
                                </a>
                                <a class="btn btn-warning btn-sm" asp-controller="category" asp-action="Edit" asp-route-categoryId="@category.Id">
                                    <i class="mdi mdi-credit-card"></i>
                                </a>
                                <form method="post" asp-controller="category" asp-action="Delete" asp-route-categoryId="@category.Id" style="display:inline-block">
                                    <button class="btn btn-danger btn-sm" type="submit">
                                        <i class="mdi mdi-delete-forever"> </i>
                                    </button>
                                </form>
                            </td>
                        </tr>
                        count++;
                    }

                </tbody>
            </table>
        </div>
    </div>
</div>



